{
  "name": "openstrength_backend",
  "version": "0.2.0",
  "description": "OpenStrength backend",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "firebase:deploy": "firebase deploy --only"
  }
}
