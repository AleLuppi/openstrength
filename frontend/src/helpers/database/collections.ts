// Collections
export enum dbCollections {
  users = "users",
  exercises = "exercises",
  programs = "programs",
  maxlifts = "maxlifts",
}

// Subcollections
export enum dbSubcollections {
  // Under "programs"
  programsSnapshots = "snapshots",
}
